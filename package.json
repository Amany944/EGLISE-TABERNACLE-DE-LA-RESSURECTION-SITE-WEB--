{
  "name": "eglise-tabernacle-backend",
  "version": "1.0.0",
  "description": "Site web de l'Église Tabernacle de la Résurrection",
  "private": true,
  "scripts": {
    "dev": "npx tailwindcss -i ./input.css -o ./CSS/tailwind.css --watch",
    "build:css": "npx tailwindcss -i ./input.css -o ./CSS/tailwind.css --minify"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "tailwindcss": "^4.2.2"
  }
}
